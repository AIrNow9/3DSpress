<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Admin</title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; max-width: 600px; margin: 2rem auto; }
    input, button { padding: 0.5rem; font-size: 1rem; }
    .error { color: red; }
  </style>
</head>
<body>
  <h1>Admin login</h1>
  <% if (error) { %><p class="error"><%= error %></p><% } %>
  <form>
    <label>password:
      <input name="pass" required>
    </label>
    <button formaction="/admin">Go</button>
    <p>Tip: add <code>?pass=YOUR_PASS</code> to the /admin URL in browser</p>
  </form>
</body>
</html>